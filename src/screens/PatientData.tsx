import * as React from 'react';
import {View} from 'react-native';

export const AllTestResults = ({navigation}) => {
    return (
        <View style={{alignItems: 'center', justifyContent: 'center'}}>

            {/* here, we should use a scrollable table
        to list all test results on this device */}

        </View>
    );
}